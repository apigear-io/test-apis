schema: apigear.module/1.0
name: custom_types
version: "1.0.0"

structs:
  - name: Vector3D
    fields:
      - { name: x, type: float }
      - { name: y, type: float }
      - { name: z, type: float }
